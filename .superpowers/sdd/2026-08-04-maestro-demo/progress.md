# SDD ledger — plan: docs/superpowers/plans/2026-08-04-maestro-demo.md
Task 1: implementer a3324f9c806bce894 (sonnet). Commit 67f909e (actuator + Prometheus + OTLP wiring, all 3 loan services; libs.versions.toml gained micrometer-registry-prometheus + opentelemetry-exporter-otlp).
Task 1 PLAN CORRECTION (coordinator ruling, implementer was right): the plan's Step 3 specified `management.otlp.tracing.endpoint` — the Spring Boot 3.x property name. Boot 4 uses `management.opentelemetry.tracing.export.otlp.endpoint`. Implementer proved it via /actuator/conditions: the 3.x name leaves OtlpTracingConfigurations.ConnectionDetails UNMATCHED, i.e. the exporter would have shipped SILENTLY INERT — same failure class as the previously-shipped inert MaestroObservabilityAutoConfiguration. Boot 4 property adopted. TASK 2 MUST USE THE BOOT 4 NAME.
Task 1 verification: maestro_* meters live at runtime on all three services (loan-application `maestro_workflows_parked 10.0` under load); full build green; loan E2E 10/10 PASS (happy 14s, orphan 15s, conditions 15s, saga 14s, crash-recovery 81s, two-node 24s, owner-kill adoption 77s, rolling restart 42s, timer-leader failover 106s, cross-node admin 122s) — the sample is undisturbed, which was the binding constraint.
Task 1: complete (commits 69095d3..7e8be69, review afadad14b52301546 PASS/PASS, ZERO findings). Reviewer independently confirmed the OTLP property correction by decompiling spring-boot-micrometer-tracing-opentelemetry-4.0.5.jar (OtlpTracingProperties binds management.opentelemetry.tracing.export.otlp; ConnectionDetails gates on .endpoint) and confirmed management.tracing.sampling.probability did NOT move in Boot 4. Meters proven genuine per service (12/13/4 names — underwriting's 4 = always-registered gauges + recovery counters, consistent with no loan reaching that desk). maestro-core, loan e2e/ and chaos harness all zero-diff. micrometer-tracing-bridge-otel added beyond the brief: ruled a justified necessary deviation, evidenced by dependency-tree diff.
Task 2: complete (commits 7e8be69..25891fa, review a8b9d65661a5d069d PASS/PASS + fix round 1 all 4 ADDRESSED via aafb75670939767c7). Two agent deaths mid-task; coordinator WIP-commit d789246 protected the work; fresh agent finished. Stack: 7 containers (all mem_limit) + 4 host JVMs (-Xmx256m), 11 topics pre-created, auto-create off. PROVEN: 3 Prometheus targets UP via /api/v1/targets; Jaeger trace d055f5961a6028a5d24d329ff11bc896 = 31 spans across 3 distinct services (the demo centrepiece, verified genuine); every dashboard metric name present in a live scrape; peak memory 2.27 GiB of ~4 GB (kafka 656 MiB largest); TWO_NODE=1 verified at 4 targets then restored.
Task 2 PRODUCT DEFECT FOUND -> ISSUE 23 (Critical, filed, library NOT changed this cycle): maestroKafkaTemplate is a bare new KafkaTemplate<>(pf) and Boot's kafkaTemplate is @ConditionalOnMissingBean, so Maestro's bean SUPPRESSES Boot's in every Maestro+Kafka app => spring.kafka.template.observation-enabled, producer.*, default-topic, transaction-id-prefix are ALL silently inert for ALL users. Separately MaestroSignalListenerBeanPostProcessor hand-builds ContainerProperties (so listener.observation-enabled cannot reach it) and never calls consumeWithTraceContext => domain-topic signals persist trace_context=NULL. docs/observability.md's "one connected trace" promise held only for maestro.signals.* while docs/cross-service.md tells users to use domain topics — scope limit now documented. Measured not argued: grep -c runWithExtractedContext = 0; signal rows NULL despite traceparent on the wire. Sample-level override kept as the demo stopgap. THIRD instance this session of "config looks right, nothing fires" (after the inert observability auto-config and the Boot 3.x OTLP property) — a deliberate audit of Maestro's bean overrides vs Boot's conditional defaults is warranted.
Task 3: implementer a399249c05f747bea (died 2x) then a50848a84c6f7ec22 finished. Commits 1d0a0d2 (coordinator WIP-protect), 5e4b449 (v2 source set), fdb66ba..b63c059 (live proof + report).
Task 3 PLAN CORRECTION #1 (RED-proven, ratified): brief's literal `version("parallel-verification", 1, 2)` is WRONG. DEFAULT_VERSION == -1, so a pre-change history resolves to -1, BELOW minSupported=1 -> UnsupportedWorkflowVersionException -> THE IN-FLIGHT LOAN FAILS, i.e. the exact opposite of the demo's claim, live on stage. Correct form shipped: version(PARALLEL_VERIFICATION, WorkflowContext.DEFAULT_VERSION, PARALLEL_VERIFICATION_VERSION) with `if (v < PARALLEL_VERIFICATION_VERSION)`. Evidence task-3-red-2-brief-literal-bounds-fail-inflight.log.
Task 3 PLAN CORRECTION #2 (affects Tasks 4/5 slides): the brief's "three verification branches (credit, employment, appraisal)" is WRONG — three concurrent branches awaiting the SAME signal name are impossible on this engine. v2 is TWO branches: verification fan-in || document collection. Slides must say "documents collect while verifications are outstanding", NOT "three-way verification fan-out".
Task 3 LIVE PROOF: in-flight loan-inflight-1785841603 completed FUNDED on the SEQUENTIAL path (0 VERSION_MARKER rows, 0 events at seq>=1000); new loan-newloan-1785841603 fanned out (VERSION_MARKER {"version":2,"changeId":"parallel-verification"} at seq 1; branch bands 5001+/6001+, parent resumes 7001 — matches the engine's p*1000+(i+1)*1000 partitioning). Branches PARK CONCURRENTLY (ParkingLot unparks both keys at 13:07:04.132; 0 InstanceStatusWriter version conflicts, 0 "already occupied") — live confirmation under the real stack that the release-blocking concurrent-branch-parking defect fixed this month is genuinely closed.
Task 3 TRACE IDS for the slide: v1 straight staircase b39b554f4c94c659f68468588295431c (20 spans); v2 fan-out 5c79c33935721eb3afe5f9ae8ca742f5 (28 spans, 3-way beneath one 10.77s run segment).
Task 3 RUNBOOK GOTCHAS (Task 4 MUST honour): (a) underwriting AUTO_APPROVEs these loan parameters — no runbook step may tell the presenter to approve from the pending queue (this aborted the first run); (b) the in-flight loan spans TWO Jaeger traces because the JVM restart splits it (pre-swap ec798a7e...); (c) Jaeger needs ~30s to flush before a trace looks complete — the runbook must build in that pause; (d) `RESTORE_V1=1 demo/scripts/v1-to-v2-move.sh` resets to v1 between rehearsals.
Task 3: complete (commits 25891fa..9b37451; review ae142cf7fc203fb10 PASS/PASS, 4 Minors all closed in fix round 1). Reviewer independently confirmed DEFAULT_VERSION=-1, the below-minSupported throw, band arithmetic from p*1000+(i+1)*1000, event-sequence-equality pins, and both trace IDs. Claim-5 (two branches not three) RULED CORRECT: SignalManager keys on workflowId+":signal:"+signalName and ParkingLot.register holds ONE waiter per key. F3 closed by ADDING the missing pin (latch-based deterministic interleaving of the CAS-loses mode, RED-proven), not by footnoting.
Task 4: implementer abe3af023ffbc9bf0 (skeleton-first ordering after a total-loss attempt), fix rounds by a47d20da7b62c8ed8. RUNBOOK.md 751 lines + preflight/reset/drive-loan/restart-loan-app/v1-to-v2-move + lib/jvm-env.sh. Review a142d2a6ee4604fc2 PASS/PASS-with-1-Critical; re-review a48e480d0b425aaac PASS on all items.
Task 4 REHEARSAL FINDINGS (why rehearsal is mandatory): scenario 2 (kill -9, the headline) COULD NOT RECOVER as written — start-services.sh aborts because the surviving 3 JVMs hold their ports; fixed via restart-loan-app.sh. preflight wasn't re-runnable; upload_doc passed args as URLs; SIX documented claims were wrong incl. "any JVM restart splits the trace" (crash loan is 10+23 spans). D6's ownership check `grep -l <id>` matched BOTH nodes (peer logs lock contention every poll) — proving nothing; now greps "Started workflow".
Task 4 CRITICAL FIXED AT THE CAUSE (coordinator ruling): headline scenario measured 250s, not the documented 5s. Root cause maestro.recovery.poll-interval default 60s, never overridden. Ruled: override to 5s from the DEMO SCRIPTS ONLY (samples' application.yml zero-diff — the loan E2E depends on it), documented as a teaching point ("as fast as you configure it to notice: 5s here, 60s default"). Result 250s -> phase1 17s + restart 5s + finish 40s. Residual 33s correctly attributed to KAFKA's session.timeout.ms (45s) holding the group member after kill -9 — named, not tuned.
Task 4 LATENT REGRESSION FOUND by the jvm-env extraction: v1-to-v2-move.sh held a THIRD copy of the JVM options MISSING the poll-interval flag, so running D1 would have left 8091 at the 60s default and silently reintroduced the 250s pause in any later scenario 2. Each -D flag now has exactly one definition in demo/scripts/lib/jvm-env.sh.
Task 4: complete (commits 9b37451..14bcbb9, review + re-review both PASS).
Task 4: minor (deferred, FINAL-REVIEW/QA TRIAGE): RUNBOOK.md:301-303 quotes Kafka join timestamps ("join requested 15:21:39, joined 15:22:15", "clean restart 3s") that are in no archived file (demo/.run/ is gitignored and overwritten). The ATTRIBUTION to Kafka session.timeout.ms is sound — reviewer checked the arithmetic — only the two timestamps are unarchived. Ruling: soften or archive; not blocking.
Task 4: minor (deferred): commit cee2c73's claim that the new D6 ownership check "verified to match the owner only" has no archived proof; the only archived run shows the OLD check matching both. The new check discriminates by construction (node B logs "Resuming workflow", not "Started workflow" — reviewer confirmed from the F2 log). Ruling: reasoned not observed; state it that way.
Task 4: minor (deferred): D1's quoted 32-36s was measured before v1-to-v2-move.sh gained the 5s poll flag. Impact nil (D1 is a graceful SIGTERM restart, not a recovery-poll path) but the figure comes from a config that no longer exists.
NOTE: 10 agent deaths so far this cycle, all "connection closed mid-response", none caused by defective work. Mitigations that work: brief-on-disk + short dispatch; write-skeleton-and-commit-FIRST; commit per item, never batch.
Task 5: implementer ad8bd037194753a3a (2 deaths; design direction captured to task-5-design-direction.md) then a34ac12159e9fcea0 across 4 bounded dispatches. 20 slides (Act I 5, Act II 6, Act III 9 incl. 2 code slides). Commits 437130c (skeleton, pre-exploration), b60ad1c, ef4c4b9, 748dd50, 1136769, abd9a42, 0391c2b, 91b717f, deead3d, c6e1dde, 895a247. Design: ledger rail (deck renders itself as an event log — its own thesis), ink-slate ground, brass accent for money/live-step, rose ONLY for failure/compensation, three type roles (mono=what the engine records, serif=what the business sees, sans=prose), all local stacks.
Task 5 OFFLINE PROOF: file:// URL in real Chrome, no server, all non-deck requests aborted — 20/20 slugs resolve after reload, full keyboard walk, presenter view renders notes + live next-slide preview, jump lands on #d4, 0 subresources fetched, 0 page errors, 0 overflow. Evidence demo/.evidence/task-5-presentation-offline-verification.log.
Task 5 GUARDRAILS HELD: slide 14 shows WorkflowContext.DEFAULT_VERSION never literal 1 (with a card explaining why); "three-way verification fan-out" appears nowhere except its own refutation; slide 9's notes forbid citing the send fan-out (identical in v1). Also caught a latent bug: the presenter clone made an unscoped `.slide` query return 21 — now scoped to `#stage > .slide`.
Task 5 SPLIT DECISION worth keeping: §2 became two slides — "the moment" and "the pause anatomy" (lock expiry -> poll -> replay -> resume, the knob, and the Kafka-not-Maestro honesty). An unexplained pause reads as a slow product; an explained one reads as a careful one.
Task 5: complete (commits 14bcbb9..1267564; review aeca17a71b44469a3 found 3 Criticals; fix round by a250bdf30c9fdf8cf, all 10 findings + 2 loose ends; re-review a63d3739ea5775105 PASS, "ship").
Task 5 CRITICALS (both invisible to the builder, both would only surface in front of a room): (C1) slide 6's event-log table was INVENTED — named validateApplication which doesn't exist, requestVerification singular, and a SIDE_EFFECT $maestro:currentTime row the v1 workflow's own Javadoc rules out — while its DO: block sent the presenter to the LIVE log in the same breath. (C2) the offline verification had been run with presenter view OFF: 8/20 slides clipped at 1440x900 and 14/20 at 1280x720 while actually presenting. Fixed via a .fit wrapper scaling only when needed (k=1 otherwise); now 0/20 in all four combinations, measured in real Chrome walking the deck by its own navigation, with deck-sha256 pinning the measurement to the current file.
Task 5 also caught: RUNBOOK.md had NO mention of the deck at all — a presenter handed only the runbook would never have found the presenter view. Now cross-referenced both ways.
Task 5: minor (deferred, QA triage): fitSlide has no floor on avail/h, so a future overfull slide shrinks silently rather than clipping — trades a silent clip for a silent shrink; mitigation is external (harness prints per-slide scale; runbook discloses 62%/68% at 720p and recommends >=1440x900).
Task 6 QA GATE: FAIL(Task 4) then PASS after fixes. Agent a50729d3820ca4796.
FAIL findings (all from running the runbook cold, exactly as written — nothing else could have found them): B1 BLOCKING — from a genuine down -v, preflight failed DETERMINISTICALLY at step 7/8 (reproduced 2x): the sample's domain-topic listener got partitions ~2.2s AFTER the throwaway loan was published and runs auto.offset.reset=latest, so the request was never consumed and the loan parked forever; re-running masked it, and the common-failures table had no row. B2 — §D5 denied SIDE_EFFECT rows that every live log shows four of. B3 — "seven containers" vs six.
FIXES (94cd829, 95c7137, d64c1d5, fb29c9f): B1 fixed AT THE CAUSE — start-services.sh blocks until the verification-gateway and underwriting consumer groups reach Stable (insight: HTTP health 200 was never group membership; Maestro's own consumers use earliest and were never at risk). B2 confirmed the EARLIER REVIEW OVER-CORRECTED: collectSignals fixes its deadline via the engine's memoized currentTime(), so the rows are real; §D2 now prints the true 18-row log and slides 6/15/18 carry them — the deck had repeated the false claim in slide 18's DO block, which QA's own grep missed because the pattern couldn't span HTML tags.
RE-VERDICT: PASS, independently re-verified at fb29c9f (not accepted from implementer logs), 13/13 quotes grepped back. Cold preflight passes FIRST attempt, partitions 3.47s BEFORE loan start (was 2.21s/2.13s late); the gate fails loudly at 90s naming the group and diagnostic command rather than dropping records silently. Peak memory 2.42 GiB of ~4 GB. Constraints zero-diff throughout: maestro-core, sample-loan-origination/e2e, chaos harness.
Timing drift closed (60792c0): warm preflight 36s->42s, reset.sh 17s->21-22s, in both RUNBOOK.md and the deck (kept in lockstep).
