# Green-Main ledger — branch worktree-green-main, base 15b27dc (merged main)

Goal: make `main` green. Two independent defects found by post-merge investigation of PR #31's red CI.

Fix 1 — intermittent Build&Test flake (run 30728290264, EnginePostgresParallelIT:149, 5 events expected/4 seen):
- Implementer a68311478409c3457 (opus). Commits 82a4a65 (repro fixture TerminalEventDelayStore), 6296427 (predicate fix + sweep + TerminalEventRaceIT pin), 67e59d2 (report + evidence).
- Root cause: engine writes instance-row terminal and appends the terminal event as TWO non-transactional calls; tests gated on status().isTerminal() then immediately read getEvents(). PR #31 had inserted an observer emit() into that gap, widening it.
- Sweep routed 9 sites through one TerminalWait predicate, INCLUDING maestro-test/TestWorkflowHandle:166 — the same defect in SHIPPED PUBLIC API, inherited by every downstream user of awaitCompletion(); getEvents().
- Review a5b96aaa5557702c5: APPROVE, pin verified load-bearing by the reviewer itself (scratch copy, reverted TerminalWait.isFinalised to status-only -> 3 of 4 failed at the event-log assertions, control still green). Evidence 5/5 spot-checks matched.
- OPEN (fix-1 round 1, not yet dispatched): Important — sweep missed maestro-core's OWN tests: WorkflowExecutorTerminalTransitionTest:131/136 and WorkflowExecutorShutdownTest:578/581 (same shape, in-memory store so window is a thread preemption not a DB round trip). Minors: release-notes line owed for TestWorkflowHandle now able to throw TimeoutException where it previously returned; TerminalWait:76 accepts EITHER terminal event regardless of observed status (safe only because deleteFailureEvents strips WORKFLOW_FAILED before retry — unstated dependency); docs/testing.md:522 still teaches the status-only poll; starter WorkflowStub:102 has the same shape in shipped main code (harmless as written).
- ENGINE RECOMMENDATION from implementer (not implemented, coordinator to rule): add WorkflowStore.finaliseInstance(instance, terminalEvent) with a both-or-neither contract. Explicitly NOT a reorder to append-then-status, which would let two converging runners both append and would expose a log with WORKFLOW_COMPLETED while the row still reads RUNNING (getRecoverableInstances would re-invoke it). User-visible harm today: "wait until terminal, then export the audit log" can archive a permanently incomplete log; admin dashboards can render COMPLETED runs with truncated timelines. Read-consistency defect, one round trip wide, self-healing.

Fix 2 — nightly E2E chaos red (run 30731056376, healAll -> awaitAllNodesHealthy timeout at 181.9s on a PARTITIONed node):
- Root cause: `docker network disconnect --force` + reconnect gives the container a NEW IP, stranding the published-port NAT that Testcontainers' baseUrl() uses. Node stays alive on Kafka/Postgres; only the harness HTTP probe is blinded. Seed-dependent (KILL9/ROLLING_RESTART heal via replace(), immune).
- Implementer ae5107c09bdc1ab64 (opus) DIED on a transient API error after committing its repro f0219f1; resumed with a precise state summary. In-flight uncommitted: ChaosCluster.java, PartitionReachabilityIT.java, new NodeAmbassador.java. Asked to justify the ambassador approach vs simply re-resolving the mapped port.
