/**
 * Spring Boot auto-configuration for PostgreSQL-based workflow messaging.
 *
 * <p>When the PostgreSQL JDBC driver is on the classpath and
 * {@code maestro.messaging.type} is {@code "postgres"}, this package's
 * auto-configuration creates the necessary beans for Postgres-based
 * messaging and signal notification.
 */
@NullMarked
package io.b2mash.maestro.messaging.postgres.config;

import org.jspecify.annotations.NullMarked;
