/**
 * Event projection for the Maestro Admin dashboard.
 *
 * <p>Contains the projector that materializes workflow lifecycle events
 * into the admin database tables for dashboard queries.
 *
 * <p>All types in this package are non-null by default.
 * Nullable fields are explicitly annotated with {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package io.b2mash.maestro.admin.projection;

import org.jspecify.annotations.NullMarked;
