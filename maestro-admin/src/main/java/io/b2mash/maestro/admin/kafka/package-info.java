/**
 * Kafka integration for the Maestro Admin dashboard.
 *
 * <p>Contains the event consumer that subscribes to workflow lifecycle events
 * published by services running the Maestro workflow engine.
 *
 * <p>All types in this package are non-null by default.
 * Nullable fields are explicitly annotated with {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package io.b2mash.maestro.admin.kafka;

import org.jspecify.annotations.NullMarked;
