/**
 * Maestro auto-configuration, configuration properties, and startup runners.
 *
 * <p>All types in this package are non-null by default. Nullable elements
 * are explicitly annotated with {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package io.b2mash.maestro.spring.config;

import org.jspecify.annotations.NullMarked;
