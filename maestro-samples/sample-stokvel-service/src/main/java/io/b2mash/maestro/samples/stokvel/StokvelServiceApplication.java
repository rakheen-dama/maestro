package io.b2mash.maestro.samples.stokvel;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Sample — Stokvel onboarding service demonstrating parallel branches,
 * signal collection, and cross-service orchestration.
 */
@SpringBootApplication
public class StokvelServiceApplication {

    public static void main(String[] args) {
        SpringApplication.run(StokvelServiceApplication.class, args);
    }
}
