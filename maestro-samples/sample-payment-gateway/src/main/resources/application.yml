spring:
  application:
    name: sample-payment-gateway

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:maestro}
    username: ${POSTGRES_USER:maestro}
    password: ${POSTGRES_PASSWORD:maestro}

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9092}
    # Consumer deserializers stay explicit: Boot's OWN kafkaListenerContainerFactory
    # (built from spring.kafka.consumer.*) backs the plain @KafkaListener bean
    # (PaymentRequestListener), which Maestro's engine-forced invariants do not
    # reach — only maestroKafkaConsumerFactory gets those. Do not remove.
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.ByteArrayDeserializer

  data:
    redis:
      url: redis://${VALKEY_HOST:localhost}:${VALKEY_PORT:6379}

server:
  port: ${SERVER_PORT:8082}

maestro:
  service-name: payment-gateway

  store:
    type: postgres

  messaging:
    type: kafka
    topics:
      tasks: maestro.tasks.payments
      signals: maestro.signals.payment-gateway

  lock:
    type: valkey

  worker:
    task-queues:
      - name: payments
        concurrency: 5

  admin:
    events:
      enabled: true
