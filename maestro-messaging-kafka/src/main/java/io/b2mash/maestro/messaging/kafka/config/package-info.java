/**
 * Spring Boot auto-configuration for Kafka-based workflow messaging.
 *
 * <p>When Spring Kafka is on the classpath and {@code maestro.messaging.type}
 * is {@code "kafka"} (the default), this package's auto-configuration creates
 * the necessary beans for Kafka publishing, consuming, and signal listener
 * processing.
 */
@NullMarked
package io.b2mash.maestro.messaging.kafka.config;

import org.jspecify.annotations.NullMarked;
